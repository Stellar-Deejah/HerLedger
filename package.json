{
  "name": "herledger-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20.19.0 || ^22.12.0 || >=24.0.0",
    "pnpm": ">=6"
  },
  "scripts": {
    "build": "pnpm --prefix herledger-app build",
    "lint": "pnpm --prefix herledger-app lint",
    "typecheck": "pnpm --prefix herledger-app typecheck",
    "test": "pnpm --prefix herledger-app test",
    "format": "pnpm --prefix herledger-app format"
  }
}
